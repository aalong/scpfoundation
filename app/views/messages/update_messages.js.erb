var last = $('.message:last');
var container = $('#messages_container');
var body = $('body');

if (last.length == 0)
  container.html("<%= j render @message %>");
else
  last.after("<%= j render @message %>");

if (container.height() - $(document).scrollTop() < 1000)
  body.scrollTop($(document).height());
